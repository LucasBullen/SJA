//
//  checklistsViewController.swift
//  SJA
//
//  Created by Lucas Bullen on 2016-02-03.
//  Copyright © 2016 Lucas Bullen. All rights reserved.
//

import UIKit

/*class ChecklistViewController: UIViewController {
    
    @IBOutlet weak var grabNGo : UIButton!
    @IBOutlet weak var vehicle : UIButton!
    @IBOutlet weak var work : UIButton!
    @IBOutlet weak var camping : UIButton!

    
    
    class ChecklistTableViewController :
    UITableViewController {
        
        let kitsDataSet =
            ["72 Hour 'Grab-N-Go' Kit for Home", "Vehicle Kit for Your Car", "Survival Kit for Work", "Camping Kit"]
        
        //number of sections of cells in the table
        override func numberOfSectionsInTableView
            (tableView: UITableView) -> Int {
                return 1
        }
        
        override func tableView(tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
            return kitsDataSet.count
        }
        
        override func tableView(tableView: UITableView, cellForRowAtIndexPath indexPath: NSIndexPath) -> UITableViewCell {
            let cell = tableView.dequeueReusableCellWithIdentifier("Basic")!
            
            cell.textLabel?.text = kitsDataSet[indexPath.row]
            
            return cell
        }
    }

    
    /*
    @IBAction func grabNGoList (sender: AnyObject){
    
    
        
    }
    */
    
    override func viewDidLoad() {
        super.viewDidLoad()
        // Do any additional setup after loading the view, typically from a nib.
    }
    
    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    
}
*/